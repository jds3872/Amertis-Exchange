import { useState, useEffect, createContext } from "react";

// const _swapContext = createContext()

const SwapContext = ({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) => {
	const love = "";

	return (
		// <>
		// 	<_swapContext.Provider value={love}>{children}</_swapContext.Provider>
		// </>
		<></>
	);
};

export default SwapContext;
